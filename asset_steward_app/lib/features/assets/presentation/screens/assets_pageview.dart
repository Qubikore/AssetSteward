import 'package:material_ui/material_ui.dart';

class AssetsPageview extends StatelessWidget {
  const AssetsPageview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Assets Page'),
      ),
    );
  }
}
